# AGENTS.md curation map (2026-07-23)

## Reason

The live file had grown to 671 lines and 5,543 words. Its 484-line current
focus mixed the active question with completed experiments, rejected controls,
run history, and superseded defaults. This happened because dated evidence was
recorded under `agents_record/` but summaries were also appended to the live
focus instead of replacing its outgoing state. The result included direct
conflicts, notably the explicit restoration of `MaxComponents=12` followed by
older chronological text calling 24 the active default.

## Lossless preservation

The complete former live file is preserved as
`agents_record/2026-07-23-AGENTS-pre-curation-snapshot.md`. No history
directory was renamed, so a history manifest/move verification was not
applicable. Existing dated records were not modified or deleted.

## Migration map

- `Introduction and global status` -> retained and condensed in live section
  1; exact former wording remains in the snapshot.
- `Project laws and work scope` -> all active package, interface, truth,
  validation, repository, branch, Git, documentation, and focused-validation
  constraints retained explicitly in live section 1.
- `Compile and run` -> retained in live section 1.
- `Current focus`: active light-tail question, current baseline, decisive
  evidence, ordered next actions, success criteria, and non-goals -> retained
  in live section 2.
- `Current focus`: exact event metrics, completed workflow comparisons,
  rejected experiments, source audits, old defaults, implementation incident
  history, long provenance catalog, and superseded focus statements -> kept in
  the complete snapshot and their already-linked dated records.

## Authority decisions

- `MaxComponents=12` is live because it is the newest explicit user-selected
  default. Statements that 24 is active are historical; 24 remains an explicit
  comparison capacity.
- `SymmetricKL`, aggregate-weight selection, identity protection, and the
  five-component conditioned BH model remain active defaults.
- Runnalls ranking, the six-component conditioned model, KL smoothing, and
  CMSSW-like smoothing remain optional/default-off research controls.

## Resume point

Compare the persistent positive-LCIO amplification set with the established
19-overshoot/18-control audit, identify a physically interpretable
surface/mode discriminator, and use the focused validation ladder before any
population test or model promotion.
